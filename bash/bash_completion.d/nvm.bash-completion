if [ -d ~/.nvm ]; then
    export NVM_DIR="${HOME}/.nvm"
    test -s "${NVM_DIR}/nvm.sh" && . "${NVM_DIR}/nvm.sh" --no-use
    test -r "${NVM_DIR}/bash_completion" && . "${NVM_DIR}/bash_completion"
fi

# vi: ft=sh
