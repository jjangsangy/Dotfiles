return {
  -- on_attach = function (bufnr)
  --   -- Jump forwards/backwards with '{' and '}'
  --   vim.keymap.set("n", "<PageUp>", "<cmd>AerialPrev<cr>", { buffer = bufnr, desc = "Jump backwards in Aerial" })
  --   vim.keymap.set("n", "<PageDown>", "<cmd>AerialNext<cr>", { buffer = bufnr, desc = "Jump forwards in Aerial" })
  --   --
  --   -- Jump up the tree with '[[' or ']]'
  --   vim.keymap.set("n", "<S-PageUp>", "<cmd>AerialPrevUp<cr>", { buffer = bufnr, desc = "Jump up and backwards in Aerial" })
  --   vim.keymap.set("n", "<S-PageDown>", "<cmd>AerialNextUp<cr>", { buffer = bufnr, desc = "Jump up and forwards in Aerial" })
  -- end
}
